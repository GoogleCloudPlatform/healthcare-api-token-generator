<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<TargetEndpoint name="TargetEndpoint-1">
    <Description/>
    <FaultRules/>
    <PreFlow name="PreFlow">
        <Request>
            <Step>
                <Name>RemoveTargetSuffix</Name>
            </Step>
        </Request>
        <Response/>
    </PreFlow>
    <PostFlow name="PostFlow">
        <Request/>
        <Response/>
    </PostFlow>
    <Flows/>
    <HTTPTargetConnection>
        <Properties/>
        <!-- IMPORTANT NOTE: YOU MUST DEPLOY THE KUBERNETES SERVICE CALLED FROM THIS TARGET BEFORE USING THIS
             TARGET ENDPOINT.  Change the "{k8s-ip-addr-or-dns-name}" variable below to either the DNS name or
             IP address of the deployed service. -->
        <URL>https://{k8s-ip-addr-or-dns-name}:8080/oauth2/token</URL>
    </HTTPTargetConnection>
</TargetEndpoint>